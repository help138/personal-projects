#include <stdio.h>

typedef unsigned char BYTE;
typedef struct bint{
	BYTE* data;
	int len;
	int sign;
} bigInt;


bigint fib(int num){
}
int main(void){
	putc(10, stdout);

	bigint num1 = (bigint){0};
	
	putc(10, stdout);
	return 0;
}
